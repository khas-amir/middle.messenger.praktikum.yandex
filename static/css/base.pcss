body {
    font-family: 'Inter', sans-serif;
    font-weight: 500;
    box-sizing: border-box;
    margin: 0;
}

*,*::after,*::before {
    box-sizing: inherit;
    font-family: inherit;
}

p {
    margin: 0;
    padding: 0;
}